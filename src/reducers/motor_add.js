export default function (state, action){
    switch (action.type){
        case "Motor_add":
       // return action.payload;
       
        break;
        default:
         return state;
    }
}